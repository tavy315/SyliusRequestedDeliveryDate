{% if order.requestedDeliveryDate is not null %}
    <div class="ui small icon message">
        <i class="calendar icon"></i>
        <div class="content">
            <div class="header">{{ 'tavy315_sylius_requested_delivery_date.form.requested_delivery_date'|trans }}</div>
            <div>{{ order.requestedDeliveryDate|format_date }}</div>
        </div>
    </div>
{% endif %}
